{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block title %}Add a Article{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Add a product</h1>
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {{ form_row(form.date) }}

    
        <button type="submit" class="btn btn-primary">Add</button>
    
    
</div>
{% endblock %}